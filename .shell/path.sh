# Setup ${PATH}

# Speciliaze if needed per OS.
#
# if [ ${IS_LINUX} ]; then
#   export PATH="${PATH}:/foo/bar"
# elif [ ${IS_MACOS} ]; then
#   export PATH="${PATH}:/foo/bar/baz"
# fi
export PATH="${HOME}/bin:${HOME}/lib:${HOME}/.rvm/bin:${HOME}/bin:${PATH}"
